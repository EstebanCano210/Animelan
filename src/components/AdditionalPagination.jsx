import React from 'react';

const AdditionalPagination = ({ currentPage, totalPages, onPageChange }) => (
  <div className="d-flex justify-content-center mt-4">
    
  </div>
);

export default AdditionalPagination;
